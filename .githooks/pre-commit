#!/bin/sh
# Block Hangul and secrets in staged changes, then lint and type check.
set -e
uv run --quiet python scripts/git_guard.py pre-commit
uv run --quiet ruff check .
uv run --quiet ruff format --check .
uv run --quiet mypy
